# Local preview only: `docker compose up --build`, then open
# http://localhost:4000. Production is the CapRover app `dashy`, built from
# the Dockerfile via captain-definition.
services:
  dashy:
    build: .
    ports:
      - "4000:8080"
